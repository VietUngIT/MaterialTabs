<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="info.androidhive.materialtabs">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/MyMaterialTheme">
        <activity
            android:name=".activity.MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activity.SimpleTabsActivity"
            android:label="@string/title_activity_container_manager" />
        <activity
            android:name=".activity.StorageInfoActivity"
            android:parentActivityName=".activity.SimpleTabsActivity" />
        <activity
            android:name=".activity.DialogMoveStorageActivity"
            android:theme="@style/Theme.UserDialog" />
        <activity android:name=".activity.DialogCustomActivity"
            android:theme="@style/Theme.UserDialog"
            android:label=""></activity>
    </application>

</manifest>